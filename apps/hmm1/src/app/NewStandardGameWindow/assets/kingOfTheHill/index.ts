import type { CheckboxAssets } from '@heroesjs/hmm1-core-ui';

import checked from './checked.jpg';
import unchecked from './unchecked.jpg';

export const assets: CheckboxAssets = {
  checked,
  unchecked,
};
